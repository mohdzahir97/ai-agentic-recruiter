# --- Web framework -----------------------------------------------------------
fastapi==0.139.0
uvicorn[standard]==0.50.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.0.1
email-validator==2.2.0
python-multipart==0.0.32

# --- Database ----------------------------------------------------------------
sqlalchemy==2.0.51
# Postgres driver. SQLite needs no driver, so the app still starts without a
# Postgres server when DATABASE_URL points at a file.
psycopg[binary]==3.2.10

# --- Auth --------------------------------------------------------------------
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# --- AI / Agents -------------------------------------------------------------
langchain==1.3.11
langchain-text-splitters==1.1.2
langgraph==1.2.11
# Only the provider package for the LLM you actually use needs to import
# cleanly; the factory imports lazily so a missing one is not a startup error.
langchain-groq==1.1.3
langchain-openai==1.3.3
langchain-google-genai==4.2.6
langchain-ollama==1.1.0

# --- RAG ---------------------------------------------------------------------
chromadb==1.5.9
pypdf==6.14.2
